import 'package:flutter/material.dart';

class VerifyDriver extends StatelessWidget {
  const VerifyDriver({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Verify Driver'),
      ),
      body: Center(
        child: Text('Verify Driver'),
      ),
    );
  }
}
